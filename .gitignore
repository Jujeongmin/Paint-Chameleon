node_modules/
dist/
*.log
.env
.env.local
.superpowers/

# TypeScript incremental build cache. Regenerated by every , so tracking
# it means both sides rewrite it on every build and every merge conflicts on a
# file nobody reads.
tsconfig.tsbuildinfo
