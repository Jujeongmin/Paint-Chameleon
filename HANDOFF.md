# 인수인계 (2026-07-28, 6차 세션)

새 세션에서 이 파일을 먼저 읽고 시작하세요. [README.md](README.md)는 아키텍처 배경(왜 이렇게
만들었는지)을 설명하고, 이 파일은 **지금 뭘 하고 있었는지**를 설명합니다.

---

## 6차 세션 (2026-07-28) — 상점 상호작용 재설계 [완료]

5차 세션이 남긴 작업 지시서를 그대로 실행했습니다. 지시서의 기하 계산은 전부 그대로
들어맞았고, 계산해둔 값과 실제 코드 사이에 어긋난 곳은 없었습니다.

### 사용자 요청 (5차 세션에서 확정)

> "상점을 로비에서 3d구조물로 배치해서 아바타 앞에 서면 구매하는 방식하고싶은데"

모달 패널이 정작 미리보기인 마네킹을 가리던 문제. 상호작용은 **[E] 한 번**으로 확정
(`AskUserQuestion`으로 선택됨 — 아바타는 영구 해금이라 확인 단계 불필요).

### 한 일

**폐기:** `src/ui/Shop.tsx`, `ui.css`의 `.shop-*` 모달 블록, `atShop()`,
`SHOP.triggerRadius`, `onShopProximity`, `shopOpen`/`shopDismissed`/`nearShop` 상태.
상점은 더 이상 이동을 얼리지 않고 커서도 필요로 하지 않습니다.

**`src/hub/hubMap.ts`** — `STAND` 상수(`spacing 2.2` / `triggerRadius 1.0` / `stepZ 1.5`),
`Stand` 인터페이스, `BODIES`에서 파생한 `STANDS`, 최근접 우선 `standAt()`. 카운터 박스 2개를
백드롭 1개(폭은 `(BODIES.length - 1) * spacing + 1.6` = 8.2로 파생)로 교체하고, 맨 왼쪽 발판
트리거를 덮던 소품 `[-12, 6, ...]`을 삭제했습니다.

**`src/hub/Hub.tsx`** — 마네킹 4개(`classic` 포함)가 `STANDS`를 순회하고, 발판마다 바닥에
트리거 원을 그립니다. 현재 장착 중인 발판은 초록으로 강조.

**`src/hub/HubPlayer.tsx`** — `standRef`에 매 프레임 기록(`portalRef`와 같은 폴링 패턴).

**`src/ui/ShopPrompt.tsx` (신규)** — 비모달 하단 프롬프트, `pointer-events: none`,
`e.repeat` 가드가 있는 `KeyE` 리스너. 리스너는 `buy`/`equip`이 실제로 가능할 때만 등록됩니다.

**`src/ui/standAction.ts` (신규, 지시서에 없던 추가)** — `[E] 한 번이 무엇을 하는가`를
순수 함수로 분리했습니다: `loading`/`equipped`/`equip`/`buy`/`broke`. 이유는 아래 "검증"
참고 — 브라우저에서 눈으로 확인할 수 없는 환경이라, 이 결정표만이라도 `check:shop`이
자동 검증할 수 있게 만들어야 했습니다.

**`src/ui/useWallet.ts` (신규)** — 지갑 + busy + 자동 소멸 메시지를 한 훅으로. 4차 세션의
교훈 유지: **`buyAvatar`가 반환한 지갑을 그대로 쓰고 절대 재조회하지 않습니다.**

**`src/ui/HubHud.tsx`** — `standRef` 폴링을 기존 60ms `portalRef` 인터벌에 합치고, 상단 바에
코인 칩 상시 표시. **`src/App.tsx`** — 상점 상태 4개가 `useWallet` 하나로. `hide-cursor`는
이제 페인트/자세 메뉴만 봅니다.

**`scripts/check-hub.ts`** — 상점 검사를 발판별로 재작성(설 수 있는 자리인지 / 스폰에서
도달 가능한지 / 트리거끼리 안 겹치는지 / 포털과 안 겹치는지). `STANDS`가 `BODIES`에서
파생되므로 **다섯 번째 체형을 추가하면 열이 넓어져 소품과 충돌할 수 있고, 이 검사가 그걸
잡습니다.** **`scripts/check-shop.ts`** — `standAction` 결정표 9개 검사 추가.

### 이어서 — 리더보드도 같은 방식으로 (사용자 요청)

> "리더보드도 아바타상점처럼 구조물처럼 표시해줘"

상점과 똑같은 처리를 했습니다. HUD 패널(`src/ui/Leaderboard.tsx`, `.leaderboard-*` CSS)을
폐기하고, 카펫 건너 상점과 대칭 위치(`x: 9.5, z: 4`)에 **기념비**를 세웠습니다.

- **`src/hub/leaderboardFace.ts` (신규)** — 판면을 그리는 순수 캔버스 코드. 컴포넌트에서
  분리한 이유는 검증입니다: 3D 씬은 애니메이션 프레임 안에서만 그려지는데 이 코드는
  프레임이 필요 없어서, **렌더러를 띄우지 않고 판면만 이미지로 뽑아 눈으로 확인**할 수
  있습니다. 실제로 그렇게 확인했습니다(아래 검증 참고).
- **`src/hub/LeaderboardBoard.tsx` (신규)** — 캔버스 텍스처를 판면 평면에 붙입니다.
  `NameTag`와 달리 빌보드가 아니고 depth test를 **합니다** — 물리적 물체의 표면이라
  가려지고 같이 돌아야 합니다. 조명 무관하게 대비를 유지하려고 `meshBasicMaterial`.
- **`src/ui/useLeaderboard.ts` (신규)** — 폴링을 컴포넌트 밖으로. 판면은 이제 월드
  지오메트리라 시선을 돌리면 언마운트되는 컴포넌트에 폴링을 두면 매번 재시작됩니다.
  `enabled`로 매치 중에는 안 돕니다.
- `hubMap.ts` — `LEADERBOARD` 상수 + 판/받침 박스. 소품 `[12, 6, ...]`도 삭제했습니다
  (판 앞을 가림). 5차 세션에 왼쪽 소품을 지웠으니 이걸로 다시 대칭이 됐습니다.
- `check-hub.ts` — 판 앞에 설 자리가 있는지 / 스폰에서 걸어갈 수 있는지 / 판이 상점 열에
  닿지 않는지. 상점 폭이 `BODIES`에서 파생되므로 마지막 검사가 의미가 있습니다.

### 검증 — 브라우저 미리보기는 이번에도 실패했지만, 이번엔 원인이 특정됩니다

`npm run check`(타입체크 + 8개 검사 스크립트 + 서버 테스트 22개) 전부 통과,
`npx vite build` 성공. `check:hub`는 발판 4개 각각에 대해 스폰에서 실제 물리로 걸어가
0.30~0.39u 안에 도달함을 확인했습니다(트리거 반경 1.0).

**시각 확인은 또 못했습니다 — 4차 세션과 같은 벽이지만 원인은 이번에 확인됐습니다.**
개발 서버는 정상이고 페이지도 뜹니다(뷰포트 1280x720, 콘솔 에러 없음, 닉네임 입력 → 허브
진입까지 접근성 트리로 확인, 코인 칩 "100 코인" 표시 확인). 그런데:

```
document.hidden === true,  requestAnimationFrame 1초에 0회
```

**브라우저 패널이 화면에 표시되지 않으면 페이지가 백그라운드 탭으로 취급되어 rAF가 아예
돌지 않습니다.** `useFrame`이 한 번도 실행되지 않으므로 이동도, `standRef` 기록도, 렌더링도
일어나지 않습니다. 합성 키 이벤트로 걸어가게 하려 했지만 같은 이유로 움직이지 않았습니다
(키 이벤트 자체는 도달합니다 — 튜토리얼 힌트가 사라지는 것으로 확인).

**단, 리더보드 판면은 실제로 눈으로 확인했습니다.** `leaderboardFace.ts`를 페이지에서
직접 import해 캔버스에 그리고 PNG로 뽑아 봤습니다 — 캔버스 2D는 애니메이션 프레임이
필요 없기 때문입니다. 확인한 것: 10위 + 내 순위 레이아웃, 내가 10위 안에 있을 때 중복 행이
안 생기는 것, 기록 없음 상태, 그리고 긴 닉네임 말줄임(60자 → 24자, 폭 722 ≤ 한계 748)과
빈 닉네임의 "익명" 폴백.

**다음 세션에서 가장 먼저 할 일:** 브라우저 패널을 화면에 띄운 상태로 `npm run dev`를 띄우고,
스폰에서 왼쪽 앞으로 걸어가 발판 4개를 눈으로 확인하세요. 확인할 것:
1. 마네킹 4개가 서로 다른 체형으로 보이는지, 백드롭 안에 들어가지 않는지.
2. 발판 위에 서면 하단 프롬프트가 뜨고 `[E]`로 구매/장착이 실제로 되는지, 코인 칩이 줄어드는지.
3. 발판 사이를 걸어갈 때 프롬프트가 깜빡이거나 두 개가 겹치지 않는지.
4. 오른쪽 리더보드 기념비의 판면 글씨가 **서 있는 거리에서 읽히는지.** 판면 자체는
   이미지로 확인했지만, 3D 공간에서 몇 미터 떨어져 볼 때의 가독성은 확인 못 했습니다 —
   안 읽히면 `LEADERBOARD.width`/`height`를 키우거나 `TEX_W`/`TEX_H`를 올리세요.

### 남은 미결

4차 세션이 남긴 미결 3개 중 **1번(패널 배경이 마네킹을 가림)은 이 작업으로 사라졌습니다.**
2번(실루엣 면적 미균일)은 설계 의도로 문서화되어 있고, 3번(시각 확인)은 위 사유로 남습니다.

---

## 4차 세션 (2026-07-27) — 아바타 상점 구현

프로젝트: Verse8/Agent8용 3D 멀티플레이어 숨바꼭질. 원작(Poki의 Hide and Paint)을 카피하지
않고 독자 구현. 로비(허브)+매치 두 씬, 자기 몸에 자유형 브러시로 페인팅, 서버 권한 페이즈 머신.

**이번 세션에 아바타 상점을 처음부터 끝까지 완성했습니다** — 지난 세션에서 브레인스토밍
중단 상태였던 기능입니다. 설계 문서, 계획, subagent-driven-development로 10개 태스크(문서
갱신 포함)를 순서대로 실행해 전부 커밋까지 마쳤습니다.

## 이번 세션에서 한 일 — 아바타 상점

**설계 문서**: [`docs/superpowers/specs/2026-07-27-avatar-shop-design.md`](docs/superpowers/specs/2026-07-27-avatar-shop-design.md)
**계획**: [`docs/superpowers/plans/2026-07-27-avatar-shop.md`](docs/superpowers/plans/2026-07-27-avatar-shop.md)
**커밋**: `2e4f85e`~`c6e6932`, 총 18개 (`git log --oneline 7bf8d33..HEAD`로 전체 목록 확인
가능). 뒤쪽 `ab0d8a0`~`c6e6932` 7개는 최종 리뷰가 잡아낸 문제들의 수정 웨이브입니다 —
그 중 `AVATAR_PRICES`가 평범한 객체 리터럴이라 `applyPurchase(w, "toString")`이 세 가지
거부 검사를 전부 통과하고 `coins: NaN`을 써버리던 실제 보안 버그가 있었습니다
(`hasOwnProperty` + `Number.isFinite` 가드로 수정).

지난 세션에서 결정하지 못했던 두 가지가 이번에 정해졌습니다:

- **아바타는 체형을 바꿉니다** (지난 세션에 기울어 있던 "텍스처 프리셋" 방향이 아니라
  체형 방향으로 결정됨). 대신 지난 세션이 우려했던 "체형이 다르면 위장 성능이 달라진다"는
  문제를 정면으로 풀었습니다 — 아래 참고.
- **화폐는 리더보드 점수와 분리된 별도의 "코인"** 입니다. 구매가 순위를 깎지 않습니다.

### 체형 4종, 전부 증명 가능하게 동일한 판정 크기

`src/game/bodies.ts`에 `classic`(기본, 무료) / `bean`(콩이, 40) / `stick`(막대, 60) /
`tank`(떡대, 90) 네 프로필. 총 키(`TOP_Y` 1.86), 발 높이(`FOOT_Y` 0.13), 최대 반폭
(`maxHalfWidth` ≤ `MOVE.playerRadius` 0.45)이 네 체형 전부 동일합니다.

- 키·발 높이는 프로필에 **저장하지 않고 파생(`derive()`)** 시켜서, 애초에 위반을 표현할
  방법이 없습니다.
- 폭과 어깨/엉덩이 피벗 위치는 저장값이라 위반이 가능하므로 `validateProfile()`이 검사하고,
  `scripts/check-bodies.ts`(`npm run check:bodies`)가 카탈로그 전체에 대해 돌립니다.
- `Humanoid.tsx`는 모듈 상수 대신 `BodyProfile`을 받아 렌더링하도록 바뀌었습니다
  (`26d25bc`).

숨바꼭질에서는 몸이 작을수록 은신에 유리하므로, 이건 우연한 일치가 아니라 **유료 아바타가
위장 성능에서 이득을 주지 않는다는 것을 코드로 증명**하려는 설계입니다.

### 코인 · 지갑 · 구매

- `COINS = { perRound: 5, survived: 5, perCatch: 2 }` (`server/src/rules.ts`). 라운드
  참여만 해도 5코인, 잡히지 않고 생존하면 +5, 술래는 잡은 명수당 +2.
- 순수 로직(`applyPurchase`/`applyEquip`/`coinsFor`/`parseOwned`/`serializeOwned`)은
  전부 `server/src/rules.ts`에 있고 `scripts/check-shop.ts`(`npm run check:shop`)가
  커버합니다.
- 서버: `wallets` 콜렉션(`WALLET_COLLECTION`), 원격 메서드 `getWallet`/`buyAvatar`/
  `equipAvatar`(`server/src/server.ts`). 구매·장착은 전부 `$lock("wallet:"+account)`로
  감싸 동시 요청 경쟁을 막습니다.
- 코인 적립은 `endRound`에 들어가 있고, 리더보드 쓰기와 **같은 계약**을 따릅니다 — 페이즈를
  먼저 `results`로 전환한 뒤 지갑 쓰기를 시도하고, 실패해도 라운드 진행을 막지 않습니다
  (커밋 `88d005c`의 그 교훈을 그대로 재사용).
- 클라: `WalletView`/`BuyFailure` 타입(`src/net/types.ts`), 오프라인 리허설 모드는 코인
  100개로 시작하는 인메모리 지갑을 흉내냅니다.

### 허브 상점 키오스크 + 구매 패널

- `src/hub/hubMap.ts`의 `SHOP`(좌표 `x:-9.5, z:4`, `triggerRadius: 3.0`)과 `atShop()`.
  스폰 지점과 포털 사이, 카펫 옆에 배치해서 일부러 찾아가지 않아도 지나가게 됩니다.
- 유료 세 체형(`bean`/`stick`/`tank`)이 실제 크기 마네킹으로 턴테이블 위에 서 있습니다
  (`0afb866`).
- 반경 3.0 안으로 걸어 들어가면 `HubPlayer`가 `onShopProximity`로 알리고 `App.tsx`가
  `Shop.tsx` 패널을 열면서 허브 플레이어를 얼립니다(`frozen`). 패널은 닫기 버튼 또는
  Escape로 닫히고(`dismissed`), 그러면 다시 움직일 수 있습니다.
- `Shop.tsx`: 구매 실패 사유는 `REFUSAL` 맵(`unknown`/`owned`/`broke`)으로 인라인
  메시지, 네트워크 예외는 조용히 재시도 유도 메시지, 미확인 폴백 없이 항상 4종 카드를
  보여줍니다.

## 아직 안 된 것 / 검증 못 한 것 — 정직하게

이번 세션에서 **브라우저 미리보기 패널이 이 환경에서 작동하지 않았습니다** (뷰포트가
0x0으로 잡히고 스크린샷이 타임아웃). 그래서:

1. **상점 마네킹 3개가 눈으로 봤을 때 실제로 서로 다른 체형으로 보이는지, 패널
   레이아웃이 의도대로 나오는지, 구매→장착 전체 플로우가 화면에서 실제로 동작하는지는
   이번 세션에서 한 번도 시각적으로 확인되지 못했습니다.** 대신 기하학적 산술(높이·폭·
   피벗 계산)과 상태 머신 트레이스(구매→장착→재구매 불가 등)로 대체 검증했고, 둘 다
   독립적으로 재검증했습니다. 하지만 이건 실제로 화면을 본 것과 같지 않습니다. **다음
   세션에서 브라우저 패널이 정상 작동하면 가장 먼저 이 세 가지를 실제로 눈으로 확인하세요.**
2. **라운드 종료 시 코인 적립은 자동 검증되지 않습니다.** `@agent8/gameserver-node`
   테스트 하네스가 라운드를 결과 페이즈까지 진행시키지 못하는 것과 같은 벽(리더보드도
   똑같은 사정)입니다. `coinsFor`의 산술은 `check:shop`이 커버하지만, `endRound`가 그
   값을 실제로 지갑에 반영하는 배선은 코드 리뷰로만 확인했습니다. 같은 이유로 **구매
   자체도 살아있는 서버 테스트에서는 재현 불가능**합니다 — 테스트가 스스로에게 코인을
   지급할 원격 메서드가 없기 때문인데(만들면 누구나 자기 잔액을 조작할 수 있게 되므로
   의도적으로 안 만들었습니다), 그래서 구매 성공 경로도 순수 로직 검증과 코드 리뷰로만
   확인된 상태입니다.
3. **`wallets` 콜렉션의 실제 배포 환경 영속성 미확인** — 리더보드 콜렉션과 정확히 같은
   사정입니다(로컬 구현은 인메모리, 서버 재시작 시 소실). 배포 후 확인 필요.
4. **실제 네트워크 멀티플레이어(두 진짜 클라이언트 간 동기화)는 이번 세션도 검증
   못했습니다 — 4세션 연속입니다.** `.env` 파일 자체가 존재하지 않아 `VITE_AGENT8_VERSE`가
   비어 있고, 앱은 항상 오프라인 리허설 모드로 뜹니다(`src/net/useGame.ts:20`). 실서버를
   붙여서 두 브라우저 탭(또는 두 기기)으로 접속해보는 것이 여전히 남아 있습니다.

README의 "3D 로비 허브 미구현" 항목은 이번 세션에서 바로잡았습니다 — 허브는 오래전부터
동작하고 있었고, 그 문구 자체가 사실과 달랐습니다. README 알려진 한계 섹션에 아바타 상점
관련 3개 항목(코인 적립 미검증, 지갑 영속성 미확인, 체형 판정 크기 불변식)도 추가했습니다.

## 다음 세션에 할 만한 일

우선순위 순:

1. **브라우저 패널을 띄운 채로** 상점 시각 확인 — 6차 세션 절의 확인 항목 3개.
2. 실제 네트워크 멀티플레이어 검증 — Verse8에 배포하거나 `VITE_AGENT8_VERSE`를 채운
   `.env`로 실서버를 붙여 두 클라이언트로 테스트.
3. README 다음 단계 항목들(허브 접속자 표시, 포털 자동매칭, Y축 이동 검증, 페인트 중간
   참여, `$roomTick` 실측, 모바일)은 전부 그대로 남아 있습니다.
4. 새 기능을 또 시작한다면 지난 4개 세션과 같은 순서로: `superpowers:brainstorming` →
   설계 문서 승인 → `superpowers:writing-plans` → `superpowers:subagent-driven-development`.
   이번 세션도 정확히 이 순서로 10개 태스크를 순서대로 실행했고, 서브에이전트 리뷰가
   최종 단계에서 실제 문제를 잡아내는 패턴이 계속 반복되고 있습니다(과거 세션 기록 참고).

## 검증 명령

```bash
npm run check          # 타입체크 + 맵/속도 동기화 + 체형 불변식 + 상점 순수 로직
                        # + 이동/카메라/점프/코너탈출 + 허브 도달성
                        # + 사운드/리더보드 순수 로직 + 서버 테스트 — 전부 통과 상태
npx tsc --noEmit        # 클린
npx vite build          # 성공
npm run check:bodies    # 체형 4종 불변식(키/발높이/반폭/어깨·엉덩이 피벗) 개별 확인
npm run check:shop      # 코인 계산·구매·장착 순수 로직 개별 확인
```

## 알려진 한계 (README.md에 최신 상태로 정리돼 있음, 요약)

1. ~~3D 로비 허브 미구현~~ — 이번 세션에 바로잡음: 허브는 이미 구현되어 있습니다. 남은
   건 접속자 표시·포털 자동매칭.
2. 수평(XZ) 이동만 서버 검증됨, Y축은 아직.
3. 페인트 중간 참여 미지원.
4. `$roomTick` 실제 주기 미측정 — 배포 후 확인 필요.
5. 모바일 미지원 / VXShop·Ads 미연동.
6. 리더보드 콜렉션의 실제 배포 환경 영속성 미확인.
7. 아바타 상점: 코인 적립 자동 검증 불가(테스트 하네스 한계), 구매도 마찬가지.
8. `wallets` 콜렉션의 실제 배포 환경 영속성 미확인.
9. 아바타 체형 판정 크기 불변식 — 설계 의도이며 `check:bodies`로 코드 차원에서 강제됨
   (한계라기보다 이번 세션이 남긴 보증에 가깝지만, 다음 체형을 추가할 때 반드시 지켜야
   할 제약이라 여기 같이 적어둡니다).
10. 실제 네트워크 멀티플레이어 미검증 — 4세션 연속.
11. 상점 UI/마네킹의 시각적 동작은 아직 한 번도 눈으로 확인되지 않았습니다. 6차 세션에서
    원인이 특정됐습니다 — 브라우저 패널이 표시되지 않으면 `document.hidden === true`가 되어
    `requestAnimationFrame`이 아예 돌지 않고, `useFrame` 기반인 이동·렌더링이 전부 멈춥니다.
    패널을 띄운 채로 다시 시도하면 됩니다. 여전히 최우선 확인 항목.
