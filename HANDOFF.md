# 인수인계 (2026-07-24, 2차 세션)

새 세션에서 이 파일을 먼저 읽고 시작하세요. [README.md](README.md)는 아키텍처 배경(왜 이렇게
만들었는지)을 설명하고, 이 파일은 **지금 뭘 하고 있었는지**를 설명합니다.

프로젝트: Verse8/Agent8용 3D 멀티플레이어 숨바꼭질. 원작(Poki의 Hide and Paint)을 카피하지
않고 독자 구현. 로비(허브)+매치 두 씬, 자기 몸에 자유형 브러시로 페인팅, 서버 권한 페이즈 머신.

**이번 세션도 브라우저 패널이 화면에 표시(compositing)되지 않았습니다** — `preview_start`로
dev 서버는 정상 기동했고 `read_page`/`computer` 클릭/console/network 조회는 다 됐지만,
`screenshot`만 "the Browser pane is not displayed"로 매번 실패했습니다. 이건 앱 UI에서 패널을
열어야 하는 클라이언트 측 문제로 보입니다 — 새 세션에서 패널이 열려 있는 상태로 시작하면 아래
"시각 확인 필요" 항목들을 몇 분 안에 끝낼 수 있습니다.

## 이번 세션에서 한 일

### 1. 눕기 자세 땅속 파묻힘 — 수정 완료, 수치 검증 완료, 시각 확인 필요

`src/game/constants.ts`의 `lie` 자세 `lift`를 `-1.02` → `0.28` → **`0.45`**로 수정했습니다.

지난 세션에서 원인은 파악됐지만(`pitch≈86°` 회전만으로 이미 몸이 바닥 높이 근처로 내려오는데,
`lift`가 그 위에 또 큰 음수를 더해 이중으로 낮춤) 정확한 값은 미정이었습니다. 이번 세션에
회전 후 각 파츠(머리 반지름 0.34, 몸통/팔/다리)의 최저점을 구체(sphere) 근사로 계산하는
검증 스크립트를 만들어 돌렸고, 가장 깊이 내려가는 지점(엉덩이+다리 끝)이 `lift=0.45`에서
지면 위 약 0.055유닛으로 나옵니다(다른 파츠는 0.1~0.27유닛 여유). 즉 **약간 뜨는 쪽으로
설계**했습니다(다시 파묻히는 것보다 살짝 뜨는 게 안전).

```
src/game/constants.ts — POSES 배열의 lie 항목, lift: 0.45
```

**아직 실제 렌더링으로 못 봤습니다.** 구체 근사는 정확한 캡슐/구 지오메트리가 아니라서 오차가
있을 수 있음 — 브라우저 패널 열리면 `npm run dev`로 눕기 자세 선택해서 눈으로 확인하고,
뜨는 정도가 어색하면 `lift`를 0.3~0.4 사이로 미세 조정하세요.

### 2. 벽 사이 낑김 — 원인 확인 + 수정 완료 + 회귀 테스트 추가

지난 세션에서 "916건이 진짜 버그인지 판정 기준(openDirs 2~10 밴드) 자체가 검증 안 됨"이라고
남긴 부분을 이어서 확인했습니다.

**대조군 실험 결과**: 아무 문제 없는 "평평한 벽 하나에 정면으로 부딪힌" 케이스도
`openDirs=9/16`이 나와서, 기존 판정 밴드(2~10)에 그대로 들어갑니다 → **916이라는 숫자 자체는
신뢰 불가로 확인**(정상적인 벽 정지가 다수 섞여 있음). 반면 진짜 아레나 벽 코너(두 벽이 만나는
지점)는 `openDirs=5/16`으로 나왔고, 원래 스크립트가 잡아낸 5개 예시 중 2개가 정확히 이
아레나 코너였습니다. 나머지 예시들을 `MAP_BOXES`와 직접 대조해보니, 소품(crate) 2개 사이에
간격 0.03유닛짜리 진짜 틈(V자 낑김 지점)이 존재하는 것도 확인했습니다.

**근본 원인**: `src/game/map.ts`의 `moveXZ`가 X축, Z축을 각각 독립적으로 열려 있는지만
확인하고 이동하는데(축 분리 충돌), 오목한 코너(소품 2개 사이 V자 틈, 또는 아레나 벽 코너)에
대각선으로 접근하면 X이동도 막히고 Z이동도 막혀서 완전히 정지합니다. 사람이 실제로 플레이할
땐 본능적으로 스트레이핑해서 빠져나가지만, "조향 없이 순수 전진만 유지"하는 시뮬레이션에서는
최대 4.7초까지 완전히 얼어붙는 걸 확인했습니다.

사용자에게 확인 후 **"코너 슬라이드 보정 추가"**로 진행하기로 결정했습니다. 처음엔 이동
방향의 ±90° 접선으로 밀어내는 방식을 시도했으나, 좌우 대칭 코너에서는 그 접선 방향도 결국
반대쪽 벽에 막혀서 두 지점 사이를 무한 진동하는 문제가 발견됨(TDD로 합성 재현 스크립트를 만들어
확인). 최종적으로 **원(플레이어)-박스 최소이동벡터(MTV) 방식**으로 교체: 막힌 지점에서 근처
벽들에 대한 침투 깊이를 계산해서 밀어내는 방향/크기를 구하고, 그 방향으로 이동이 가능하면
적용합니다. 이 방식은 진행 방향이 아니라 순수하게 지오메트리에만 의존해서 매 프레임 같은 결과가
나오므로 진동하지 않습니다.

```
src/game/map.ts
  — isWallAt() 헬퍼 추출(playerBlockedAt 높이 판정 로직 재사용)
  — moveXZ()에 xBlocked && zBlocked일 때 MTV 기반 코너 탈출 로직 추가
```

**검증**: 실제 문제 사례(맵의 진짜 좌표, `start (5.0,12.3) heading 3.69`)로 수정 전/후
비교 — 3.75초 정지 → **0.42초**로 개선. 4000회 무작위 스윕에서 "1.5초+ 정지" 건수는
916 → 653으로 감소(단, 이 숫자엔 대조군에서 확인했듯 정상적인 평평한 벽 정지와, 진짜
막다른 방 코너처럼 순수 전진 입력으로는 원리적으로 못 빠져나가는 케이스도 섞여 있어서
0에 도달하는 게 목표가 아님 — 완벽한 대칭 45° 방 코너는 여전히 물리적으로 타당한 정지
위치에 머뭅니다, 오차 없이 벽에 박히진 않지만).

`scripts/check-movement.ts`에 "corner escape" 섹션으로 이 실사례를 영구 회귀 테스트로
추가했습니다(1초 미만 조건). `npm run check` 전체 통과, `npx tsc --noEmit` 클린, `npx vite
build` 성공 확인 완료.

**시각 확인 필요**: 실제 렌더링에서 이 밀어내기가 부자연스러운 "튐"으로 보이는지 확인 필요.
현재 밀어내는 거리는 그 프레임에 막힌 이동 스텝과 같은 크기로 스케일링해뒀지만(순간 이동처럼
보이지 않게), 실제 화면에서 봐야 확실합니다.

### 3. 점프+자세메뉴 공중 정지 — 지난 세션에 수정 완료, 이번 세션도 시각 재확인 못함

지난 세션에 `LocalPlayer.tsx`의 `stepMotion(..., { locked: held })` → `{ locked: charLocked
}`로 수정 완료된 상태를 이번 세션에도 코드로 재확인했습니다(그대로 남아있음, 정상). 로직
검증(`npx tsx` 스크립트)은 지난 세션에 끝났고, **브라우저에서 실제로 점프 중 자세메뉴 눌러서
계속 낙하하는지는 이번 세션도 확인 못했습니다.**

## 다음 세션 시작 시 우선순위

브라우저 패널이 열려 있다면(이번/지난 세션과 달리):

1. `npm run dev`로 눕기 자세 확인 → `lift` 값 미세조정
2. 점프 중 자세메뉴(G) 눌러서 공중에서 계속 낙하하는지 확인
3. 실제로 소품 사이나 코너에 걸어가서 코너 탈출 로직이 자연스러운지 확인(너무 갑작스럽게
   밀리면 `src/game/map.ts`의 push 스케일을 줄이는 것 고려)

패널이 이번에도 안 열리면, 셋 다 논리적으로는 검증된 상태이므로 우선순위 낮음 — 사용자 실사용
리포트를 기다리는 게 나을 수 있음.

## 정리된 임시 파일

지난 세션에 남아있던 `scripts/_scratch-wedge*.ts` 전부와 이번 세션에 만든 임시 조사/재현
스크립트(`_scratch-corner-*`, `_scratch-real-repro.ts`, `_scratch-wedge-sweep.ts`, `_scratch-
lie-height.ts`, `_scratch-inspect.ts`)는 조사가 끝나서 전부 삭제했습니다. 유용했던
회귀 검증 하나만 `check-movement.ts`에 영구 편입했습니다. 현재 `scripts/`에는 `check-*.ts`
셋만 남아 있습니다.

## 검증 명령

```bash
npm run check          # 타입체크 + 맵 동기화 + 이동/카메라/점프/코너탈출 + 허브 도달성 + 서버 테스트
npx tsc --noEmit        # 이번 세션 마지막 확인: 클린
npx vite build          # 이번 세션 마지막 확인: 성공, ~380KB(gzip 122KB) + three.js 683KB(gzip 176KB)
```

이 시점 기준 전부 통과 상태입니다. 위 "시각 확인 필요" 항목들은 로직 버그가 아니라
렌더링에서 눈으로 봐야 하는 미세조정/최종 확인 문제라 `npm run check`로는 안 걸립니다.

## 알려진 한계 (README.md에도 있음, 요약)

- 이동이 서버 검증 안 됨 (클라 위치를 그대로 신뢰) — 치팅 가능
- `$roomTick` 실제 주기 미측정 (배포 후 확인 필요)
- 사운드 없음, 모바일 미지원, VXShop/Ads 미연동
- 포인터 락이 이 사용자 환경에서 거부됨 — 원인 미확인. 커서 숨김+중앙 크로스헤어로 우회했지만
  화면 가장자리에서 시점 회전이 막히는 근본 한계는 남아있음
