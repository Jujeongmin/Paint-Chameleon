# 인수인계 (2026-07-30, 8차 세션)

새 세션에서 이 파일을 먼저 읽고 시작하세요. [README.md](README.md)는 아키텍처 배경(왜 이렇게
만들었는지)을 설명하고, 이 파일은 **지금 뭘 하고 있었는지**를 설명합니다.

---

## 8차 세션 (2026-07-30) — 술래 사격 무기 [Task 1~6 완료, 화면 확인은 0건]

설계 문서: [`docs/superpowers/specs/2026-07-29-seeker-gun-design.md`](docs/superpowers/specs/2026-07-29-seeker-gun-design.md).
계획: [`docs/superpowers/plans/2026-07-29-seeker-gun.md`](docs/superpowers/plans/2026-07-29-seeker-gun.md), 여섯 태스크.
커밋 전체 목록: `git log --oneline 8470638..HEAD`.

### 태그 → 사격으로 바꾼 이유

7차 세션에서 아레나가 44×44에서 88×88로 네 배 넓어지면서, 술래가 하이더를 "찾는 것"과
"잡는 것"이 별개의 노동이 됐습니다. 2.6u까지 걸어가 클릭해야 하는 옛 태그는 넓어진 맵에서
찾아낸 사람에게 다시 다가가는 시간을 추가로 요구했고, 그 추가 시간 동안 하이더가 다시
숨을 수 있었습니다. 사거리 무제한 히트스캔으로 바꿔 "찾았으면 그 자리에서 끝난다"로
만든 것이 이 세션의 요지입니다. 그 대가는 아래와 README 알려진 한계 13·14번에 그대로
적어뒀습니다 — 공짜가 아닙니다.

### 명중 판정이 클라이언트인 이유, 그리고 그 대가

서버는 맵을 갖고 있지 않습니다 — 7차 세션이 `MapBox`/`buildMap`/`isOpen`을 전부 지웠고
(`server/src/rules.ts`), 남은 건 손으로 고른 `SPAWN_POINTS` 목록뿐입니다. 벽에 막혔는지
판정하려면 맵이 있어야 하고, 맵을 든 쪽은 클라이언트뿐이므로 명중 판정 자체를 클라이언트로
넘겼습니다(`game/src/game/useShoot.ts`). 그 대가가 README 13번입니다: 서버에 남은 검사는
페이즈·역할·대상(미포획 하이더인지)·쿨다운·시야각뿐이고, **거리와 시야는 더 이상 검사하지
않습니다.** 계정 이름만 알면 맵 어디서든 잡았다고 주장할 수 있고, 서버가 맵을 다시 갖지
않는 한 되찾을 수 없습니다 — 벽을 못 보는 서버가 지금 고르는 어떤 거리 상한도 근거 없는
임의값이기 때문입니다.

### 벽 판정을 별도 계산 없이 공짜로 얻는 법

레이캐스트는 **가장 먼저 닿는 것 하나만** 돌려줍니다(`useShoot.ts`의 `intersectObjects`
결과에서 첫 히트만 씀). 엄폐물은 그 뒤에 있는 사람보다 항상 카메라에 더 가까우므로, 벽이
있으면 레이는 사람이 아니라 벽에서 멈춥니다. 그래서 "명중"과 "벽 판정"이 하나의 계산으로
합쳐지고, 따로 시선 차단 테스트를 만들어 그 테스트 자체가 틀릴 위험이 아예 없습니다.

### 계획대로였다면 총이 완전히 작동하지 않았을 것입니다

계획서는 `RemotePlayer`의 몸통 그룹에만 `userData.account`를 달게 시켰습니다 — 레이캐스트가
히트 지점에서 위로 걸어 올라가며 계정을 찾는 대상입니다. 그런데 로컬 플레이어 자신의 몸에는
그 태그가 없었고, 3인칭에서는 카메라가 플레이어 등 뒤에 있어 **거의 모든 사격에서 레이가
자기 몸통을 먼저 관통**합니다. 계정을 못 찾으니 `null`이 되어 빗맞음으로 읽히고, 모든 사격이
자기 등에서 죽었을 것입니다 — 총이 통째로 비기능이었을 뻔했습니다. Task 4 착수 전, 컨트롤러가
계획서를 미리 훑다가 잡았고, `LocalPlayer.tsx`에도 같은 태그를 달아 고쳤습니다(`6409794`).
**이번이 두 세션 연속으로 같은 부류의 계획 결함입니다** — 기능이 통째로 죽는 결함이 실행 전에
잡히지 않을 뻔했다는 점에서 — 직전 사례는 7차 세션의 `camera.ts`에 남아 있던 `y = 0` 평면이었습니다.

### `canShoot`은 sender 누락에 fail-open이었고, 그 안전 논증 자체가 고정돼 있지 않았습니다

구현자가 명시적 `!me` 가드를 지웠습니다 — 기존 서버 테스트("shot is refused outside the
seeking phase")를 깨뜨렸기 때문입니다. 대신 `me?.pos ?? [0,0,0]`, `rotY` 0, `lastShotAt` 0을
`canShoot`에 그대로 흘려보냈습니다. 이 기본값들은 중립적이지 않습니다: `lastShotAt` 0은
쿨다운 비교를 항상 통과시켜 쿨다운을 사실상 꺼버리고, `[0,0,0]`은 아레나 정중앙이라 yaw 0과
합쳐지면 맵의 +Z 절반 전체에 대해 시야각을 통과시킵니다. "`canShoot`이 phase/role을 먼저
검사하니 안전하다"는 논증은 맞지만, `check:shot`의 케이스들이 각각 유효한 요청에서 **정확히
하나씩만** 망가뜨리는 방식으로 짜여 있어서, 그 검사 순서를 실제로 고정한 적이 없었습니다 —
순서를 바꿔도 전부 그대로 초록이었을 것입니다. `sender_missing`이라는 거절 사유를
`canShoot` 안에 접어넣고(`7d5aefa`), 세 축이 동시에 틀린 케이스들을 추가해 순서 자체를
고정했습니다.

### 서버 테스트가 실제로 증명하는 것은 이름보다 적습니다

`@agent8/gameserver-node`의 `RoomContext.roomId`는 `'test-room'`으로 하드코딩돼 있고, 유일한
setter인 `setRoomId`는 어디서도 호출되지 않습니다. 반면 `joinGame`은 `$global.joinRoom`을 통해
`room_<timestamp>`에 입장시킵니다. 그 결과 `$room.getAllUserStates()`는 **항상 다른, 텅 빈
방**을 읽습니다 — `me`와 `target`은 모든 서버 테스트에서 영구히 부재합니다. "shot is refused
outside the seeking phase" 테스트가 통과하는 건 join한 방이 실제로 로비 페이즈라서가 아니라,
`state.phase ?? "lobby"`가 `"lobby"`로 떨어지기 때문입니다. 이건 오래전부터 있던 "하네스가
방을 seeking 페이즈로 못 돌린다"는 메모의 구체적인 메커니즘이고, 같은 이유로 **`canShoot`의
target 검사 이후로는 어떤 서버 테스트도 도달할 수 없습니다** — `check:shot`이 순수 함수로
따로 뽑아낸 이유가 이것입니다.

### 리뷰가 잡은 조준 회귀 두 가지

계획서 자체의 코드에 있었습니다.

1. 잠금 해제 상태의 사격이 OS 커서를 조준점으로 삼았습니다. 하지만 `Hud.tsx`는 화면 중앙에
   고정된 크로스헤어를 그리고, `ui.css`는 그 크로스헤어가 조준점이라고 명시합니다. 게다가
   자유 시점(`input.ts`)은 맨 `mousemove`만으로 카메라를 돌리므로 커서와 카메라는 계속
   어긋납니다. 옛 태그 코드는 두 잠금 상태 모두에서 yaw로 조준했으니 이건 회귀였습니다 —
   화면 중앙 고정으로 되돌렸습니다(`6f33227`).
2. Esc 이후 포인터 락을 재획득하는 클릭이 동시에 실사격을 쐈습니다. `requestPointerLock()`이
   비동기라, 같은 클릭 이벤트에서 `fire()`가 돌 때 `pointerLockElement`가 아직 캔버스가
   아니기 때문입니다. Esc 후 클릭 재획득은 이 프로젝트가 공식 지원하는 왕복 동작입니다. 옛
   2.6u 사거리에서는 무해했지만, 무제한 사거리에서는 그 한 클릭이 맵 반대편 하이더를 죽이고
   쿨다운까지 소모시킬 수 있습니다. `everLocked` 플래그로 고쳤습니다(`6f33227`).

### 로컬 피드백이 서버의 답을 거짓말하고 있었습니다

모든 클릭이 트레이서를 그리고 총소리를 최대 볼륨으로 재생했는데, 서버는 700ms 안의 요청을
거절하고 클라이언트는 그 판정 결과를 아예 버리고 있었습니다 — 거절된 사격도 화면과 소리는
"명중했다"처럼 보였다는 뜻입니다. `useShoot`가 이제 `game/src/game/constants.ts`의
`SHOT.cooldownMs`를 그대로 거울처럼 따라가서, 거절될 클릭은 트레이서도 소리도 RPC도 내보내지
않습니다(`cd63a10`). 이 상수는 그 전까지 `game/src` 안에 실제 소비자가 하나도 없었고
`check:sync`만 읽고 있었습니다 — 이제서야 첫 소비자가 생겼고, `check:sync`가 클라/서버
`SHOT` 값을 계속 대조하는 한 이 거울은 계속 정확합니다.

### 화면으로 확인 못 한 것

**이 기능은 화면에서 한 번도 확인되지 않았습니다.** 총도, 트레이서도, 명중도, 빗나감도,
엄폐물이 사격을 막는 것도, 총소리도 — 전부입니다. 지난 세션이 남긴 술래의 지하 대기실
탈출(숨기 → 추적 전환 시 순간이동)도 여전히 미확인입니다. 브라우저 패널을 열려는 시도가
이번 작업 중 두 번 있었지만 이 환경에서 표시되지 않았고, 6차 세션이 이미 기록했듯 패널이
표시되지 않으면 페이지가 백그라운드 탭이 되어 `requestAnimationFrame`이 초당 0회, R3F는
마운트조차 하지 않습니다. **확인했다고 쓰지 않습니다.** 대신 확인한 것과 그 방법만 아래에
정확히 적습니다.

이번 세션에서 실제로 한 검증은 전부 스크립트·리뷰 수준이고, 화면 확인은 0건입니다:

- `npm run check` 통과 — `tsc --noEmit` + 검사 스크립트 전부(신규 `check:shot` 포함) +
  서버 테스트 22개. `npx vite build` 성공.
- `scripts/check-shot.ts`(신규)가 `canShoot`의 결정표 전체를 덮습니다: 모든 거절 사유,
  `SHOT.cooldownMs`에서 파생한 쿨다운의 정확한 경계, `SHOT.minFacingDot`에서 파생한 시야각의
  정확한 경계, 시야각의 사방(전/후/좌/우) 전부, 비유한(non-finite) 입력.
- `scripts/check-sync.ts`가 클라이언트 `SHOT`과 서버 `SHOT`을 대조해서, 쿨다운과 시야각
  임계값이 서로 어긋날 수 없게 고정합니다.
- `scripts/check-audio.ts`가 총소리 거리 감쇠를 고정합니다: 양 끝값, 단조성, 가청 범위
  전체를 훑는 상한 스캔, 가청 범위 밖의 무음, NaN·음수 거리의 무음, 그리고 2차 곡선이
  선형으로 납작해지면 빨간불이 되는 모양 검증까지.
- **이번 작업에서 새로 짠 모든 검사는 신뢰하기 전에 빨간불부터 봤습니다** — 대상을 일부러
  깨뜨려 해당 단언이 실제로 실패하는 것을 확인한 뒤에야 통과 상태를 믿었습니다. 이 관례가
  있는 이유는, 이 저장소 역사에 **절대 실패할 수 없게 짜인 검사가 넷** 있었기 때문입니다
  (7차 세션 "계획서가 지시한 검사 두 개가 통과할 수 없거나 실패할 수 없었습니다" 참고).

### 아직 알려지지 않았거나 미검증인 것 — 그대로 적어둡니다

- **원거리 술래에게는 총이 안 보입니다.** `held`는 `LocalPlayer`에만 넘겨지고 `RemotePlayer`는
  이번 계획의 범위 밖이었으므로, 하이더 눈에는 술래가 총을 안 든 것처럼 보입니다.
- **빗나가거나 거절된 사격은 다른 사람에게 아예 조용합니다.** 클라이언트는 빗나간 사격을
  서버에 보내기 전에 버리고, 서버는 `canShoot`을 통과한 사격만 방송합니다.
- **몸이 페이드아웃될 때 총은 같이 사라지지 않습니다.** 알파는 몸의 공유 머티리얼에
  걸려 있고 GLB 총 모델의 머티리얼은 거기 속하지 않습니다 — 추적 카메라가 당겨 들어오며
  몸이 흐려져도 불투명한 총이 그 앞에 남을 수 있습니다.
- **클라이언트 쿨다운 거울은 자기 `pointerdown` 시각을, 서버는 RPC 도착 시각을 기준으로
  잽니다.** 네트워크 지터가 있으면 경계 근처의 아주 좁은 구간에서, 서버가 거절할 사격에
  대해서도 클라이언트가 트레이서를 그릴 수 있습니다.

### 다음 세션이 할 일 — 구체적으로

계정 2개, 둘 다 준비 완료, 한쪽이 술래로 배정된 상태에서:

1. 숨기 페이즈가 끝나는 순간 술래가 지하 대기실에서 아레나 중앙 `[0,0,0]`으로 순간이동하는지
   지켜볼 것 — 7차 세션부터 미확인입니다.
2. 트인 곳에 서 있는 하이더를 쏴서 실제로 잡히는지 확인할 것.
3. 상자나 칸막이 뒤에 있는 하이더를 쏴서 안 맞는지 확인할 것.
4. 트레이서가 보이는지 확인할 것.
5. 다른 계정이 맵 반대편에서 쏘는 소리가 가늘게 들리는지 확인할 것.
6. 700ms 안에 두 번 클릭해서 두 번째 클릭이 정말 아무것도 하지 않는지 확인할 것.

---

## 7차 세션 (2026-07-29) — 아레나 재설계 + 술래 대기실 [완료]

두 계획을 이어서 실행했습니다: [`docs/superpowers/plans/2026-07-28-arena-redesign.md`](docs/superpowers/plans/2026-07-28-arena-redesign.md)와
[`docs/superpowers/plans/2026-07-29-seeker-holding-cell.md`](docs/superpowers/plans/2026-07-29-seeker-holding-cell.md).
커밋 전체 목록: `git log --oneline 238a7c8..c621ed4` (이번 문서화 커밋 앞까지).

### 아레나 재설계

절차적으로 흩뿌리던 상자 맵을 손설계 맵으로 바꿨습니다. 태스크 1~6을 순서대로 실행:

1. **`poseSilhouette` 추출** (`238a7c8`) — `Humanoid.tsx`의 정지 자세를 순수 수학으로
   재현해 포즈별 바운딩 박스를 계산. 사물 패밀리 치수가 여기서 파생될 예정이었습니다
   (후에 에셋 모델 도입으로 이 파생 관계는 끊어집니다 — 아래 "방향 전환" 참고).
2. **`arena.ts` 분리** (`71d276e`) — `map.ts`가 맡던 "맵 내용"과 "충돌 판정"을 나눔.
   동작 변화 0, `check:sync`가 그대로 통과하는 것으로 검증.
3. **서버 맵 사본 삭제, `SPAWN_POINTS` 도입** (`cd0d0a9`) — 서버가 맵을 통째로 들고 있던
   유일한 이유가 "하이더를 빈자리에 스폰"이었는데, 손으로 고른 스폰 지점 목록만 있으면
   충분해서 서버의 `MapBox`/`buildMap`/`isOpen`이 전부 사라졌습니다.
4. **`check:map` 신설** (`5713b66`) — 배치보다 먼저 불변식(빈 슬롯 존재·도달성, 중앙에서
   네 구역으로의 시선 차단, 계단 규칙)을 검사로 못박음. 이 시점엔 구역이 없어 일부러 빨간
   상태로 커밋.
5. **관문 — 드럼통 구역 하나만 배치** (`de9b440`) — 설계 전체가 "칠하고 자세 잡은 사람이
   멀리서 사물로 보이는가"에 걸려 있었고, 한 번도 확인된 적이 없어 이 태스크를 관문으로
   뒀습니다. **통과했습니다 — 사용자가 직접 화면에서 확인했습니다.**
6. **나머지 3구역 + 풍차 칸막이** (`9172a5e`) — 관문 통과 후 화물상자·팔레트·선반기둥
   구역과 칸막이 8장을 채움. `check:map`이 전부 초록.

### 방향 전환 세 가지

계획에는 없던, 구현 도중 사용자 요청으로 생긴 전환입니다.

1. **미미크리 폐기.** 위 관문은 통과했지만, 사용자가 "코드로 만든 박스처럼 보인다"며
   상자 대신 에셋 모델을 요구했습니다(`ba2aa4e`). 텍스처를 입힌 모델은 단색 페인트로
   흉내낼 수 없으므로, 사물인 척하기라는 설계의 핵심 전제 자체를 버렸습니다. 위장은
   이제 엄폐와 시야 차단에서 나옵니다. 손설계 배치·빈 슬롯·풍차 칸막이·`check:map`은
   폐기되지 않았습니다 — 근거만 바뀌었습니다. **자세한 내용과 남은 것의 목록은
   [`docs/superpowers/specs/2026-07-28-arena-redesign-design.md`](docs/superpowers/specs/2026-07-28-arena-redesign-design.md)
   맨 위 후기(postscript)에 있습니다. 그 문서를 읽을 땐 반드시 후기부터 읽으세요 —
   섹션 2(미미크리)를 살아있는 설계로 오해하게 됩니다.**
2. **에셋 모델 도입** (`ba2aa4e`, `4dc5108`, `53c245b`) — Kenney 킷의 GLB 모델로 구조물과
   소품을 그림. 콜리전은 그대로 축대칭 박스(`MapBox`)이고 렌더만 모델입니다. 모델은
   박스에 맞춰 늘이지 않고 박스를 모델 치수에서 파생시켰습니다(`4dc5108`) — 모델이
   콜리전 안에서 헛도는 것이 늘어난 모델보다 더 어색하기 때문입니다. 랜드마크 건물
   8개와 시드 난수 소품 440개를 추가로 흩뿌렸습니다(`53c245b`).
3. **맵 4배 확장** (`4c29bb6`) — 44×44 → 88×88. 대각선이 62u에서 124u로, 하이더 속도
   기준 횡단 시간이 10초에서 약 21초로 늘었습니다. 페이즈 시간(숨기 30초/술래 90초)은
   옛 44×44 기준값 그대로라 **재검토가 필요합니다** (README 알려진 한계 11번).

### 셀이 처음엔 아예 작동하지 않았습니다 — 최종 리뷰가 잡음

셀 작업의 첫 태스크가 `groundHeightAt`의 `let best = 0`을 `floorY` 인자로 뽑아냈는데,
**같은 암묵 평면이 `game/src/game/camera.ts`에 두 군데 더 있었습니다** — `cameraBlockedAt`의
`if (y < CAMERA_FLOOR)`와 `cameraInsideSolid`의 `if (y < 0)`. 태스크마다 붙인 리뷰는 자기
diff만 보므로 이걸 볼 수 없었고, 전체 브랜치 리뷰에서야 나왔습니다.

결과는 기능이 통째로 죽는 것이었습니다. 셀 안(피벗 y ≈ -6.65)에서 `clearCameraDistance`가
첫 샘플에서 막혀 0을 돌려주고, 카메라가 플레이어 머리 안에 들어갑니다. 일반 플레이에서는
`bodyFadeFor(0, …)`이 0이라 **30초 동안 자기 몸이 안 보이고**, 페인트 모드에서는 카메라가
몸통 안이라 레이캐스트가 FrontSide 메시 내부에서 아무것도 맞히지 못해 **모든 붓질이
orbit으로 빠집니다.** 칠하기 연습이 이 기능의 존재 이유였는데 그게 죽어 있었습니다.
피치가 1.1rad로 clamp돼 있어 빠져나갈 각도도 없습니다.

`floorY`를 `camera.ts`·`followCamera.ts`까지 이어 붙여 고쳤고(`413842d`), 셀 피벗에서
허용 거리가 `CAMERA.fadeStart`(2.1)를 넘는지 `check:cell`이 고정합니다(실측 2.60).

**교훈**: 암묵 상수를 인자로 뽑을 때 같은 상수가 몇 군데 더 있는지 먼저 세십시오.
`grep -n "y < 0\|< CAMERA_FLOOR"` 한 번이면 됐습니다.

### 계획서가 지시한 검사 두 개가 통과할 수 없거나 실패할 수 없었습니다

- 지하 바닥 검사의 합성 박스가 `STEP_HEIGHT` 0.45를 0.05 넘겨서 **원문대로면 절대 통과
  불가능**했습니다(`ed48bc9`에서 계획서 수정).
- 셀 밀봉 검사가 **지오메트리와 무관하게 통과**했습니다. `CELL_HALF = CELL_INNER/2 +
  playerRadius`이고 `moveXZ`가 `worldHalfSize - radius`로 clamp하니 반지름이 상쇄되어,
  벽을 지우든 비우든 10u 밖으로 옮기든 늘 같은 자리에서 멈췄습니다(`cbbe035`).
- 천장 검사 `CELL_HEIGHT > TOP_Y + JUMP_APEX`는 `CELL_HEIGHT`를 그 식에서 파생시킨
  순간 `0.2 > 0`이 되어 **항등식**이 됐습니다. 상수는 자기 자신을 검사할 수 없습니다.
  실제 점프를 시뮬레이션해 머리 높이를 `CELL_BOXES`의 천장 밑면과 비교하도록 교체했습니다.

**교훈**: 검사를 적는 것과 그 검사가 실패할 수 있음을 보이는 것은 다른 일입니다. 새 검사는
검사 대상을 일부러 깨뜨려 빨간불을 본 뒤에 믿으십시오.

### 런타임에서만 나온 버그 세 개 — 코드 리뷰로는 안 나오는 종류

1. **`Arena.tsx`와 `arena.ts`가 충돌.** Windows는 대소문자를 구분하지 않는 파일시스템이라
   두 파일이 같은 이름으로 보였고, `tsc`가 프로그램 전체를 거부했습니다. 컴포넌트를
   `ArenaScene.tsx`로 개명(`71d276e`) — 이름도 "맵이 아니라 씬"이라는 걸 더 잘 말해줍니다.
2. **drei의 `useTexture`가 `<Arena>`의 모든 훅을 깨뜨림.** import하자 Vite가
   `@react-three/fiber`를 두 번 pre-bundle했고, 그 결과 `<Arena>` 안 모든 훅이
   `Invalid hook call`을 던졌습니다 — 에러가 가리키는 파일과 실제 원인(모듈 해석)이
   전혀 다른 종류의 버그였습니다. R3F 자체의 `useLoader`로 바꾸고
   `node_modules/.vite`를 비워 해결(`8c47b0e`).
3. **스포이드가 엉뚱한 색을 돌려줌.** `GLTFLoader`는 `texture.flipY = false`로 설정합니다
   (glTF는 uv 원점이 좌상단이라서). `TextureLoader`는 기본값이 `true`입니다. 한쪽만
   가정하고 텍셀 행을 읽으면 모델 텍스처가 세로로 뒤집혀 읽히는데, Kenney 컬러맵은 작은
   단색 패치가 빼곡한 아틀라스라 뒤집힌 행이 완전히 다른 패치에 떨어져 "드럼통을 찍었는데
   드럼통이 아닌 색이 나온다"로 보였습니다. 바닥·벽은 `TextureLoader`를 쓰므로 처음부터
   멀쩡했습니다. 지금은 `map.flipY`를 읽어 행을 고릅니다(`a5596ed`).

### 에셋 규칙

`public/`은 전부 그대로 배포에 실립니다(Vite가 손대지 않고 그대로 서빙). 84MB로 들어온
원본(2K 텍스처 9장 + FBX/OBJ/blend 사본 포함 Kenney 모델 143개)을 4.9MB로 줄였습니다
(`8c47b0e`) — 텍스처는 1024·품질 82로 재인코딩, GLB만 남기고 나머지 포맷은 삭제. 킷마다
`Textures/colormap.png`를 **상대 경로**로 참조하므로 두 킷의 폴더를 섞으면 뒤 킷이 앞
킷의 팔레트를 조용히 뒤집어쓰게 됩니다 — 폴더를 절대 합치지 마세요. 자세한 건
[`public/README.md`](public/README.md).

### 술래 대기실

문제 정의부터 정정했습니다. 초안은 "술래가 숨는 걸 다 본다"를 문제로 적었지만 사실이
아니었습니다 — 블라인드 오버레이(`Hud.tsx`)와 `frozen`이 이미 술래의 시야와 이동을
막고 있었습니다. **진짜 문제는 술래가 45초 동안 검은 화면만 보고 있었다는 것**이었고,
그래서 이 작업이 사주는 건 공정성이 아니라 그 시간에 할 일입니다.

- **`floorY` 인자화** (`f6434b4`) — `groundHeightAt`이 `let best = 0`으로 시작해서 y=0
  아래는 전부 표면으로 끌어올려졌습니다. 지하 방을 두려면 바닥 높이를 인자로 받아야
  했고, 기본값 0이라 아레나·허브는 그대로입니다.
- **셀 지오메트리 + `check:cell`** (`6ea194a`) — y=-8의 밀봉 방. 6개 슬래브(바닥·천장·벽
  4장), `check:cell`이 밀봉·바닥 안착·아레나와의 비충돌·순간이동 착지점을 검사.
- **클라이언트 — 셀을 그리고 블라인드를 없앰** (`747e615`, `0166dbd`) — 오버레이 삭제가
  이 작업의 핵심 산출물입니다. 셀만 만들고 오버레이를 남기면 화면상 아무것도 안
  바뀝니다. 마운트 시 로비 스폰을 덮어쓰던 버그를 리뷰에서 잡아 고쳤습니다.
- **서버 — 셀에 스폰, 전환에서 순간이동** (`eaea803`) — `hiding → seeking`에서 술래를
  `[0,0,0]`으로 옮기며 `lastMoveAt`도 같이 초기화합니다. 안 그러면 서버 자신이 만든
  순간이동을 서버가 치트로 판정해 clamp합니다.
- **오프라인 봇 제거** (`c621ed4`) — 사용자 결정: AI는 술래가 될 수 없습니다. 온라인
  방은 사람만 들어오니 문제없지만, 오프라인 리허설은 봇도 술래 후보였습니다. 봇을 아예
  없애 혼자면 항상 술래가 되게 해서 규칙을 저절로 지키게 했습니다. 대신 오프라인에서는
  숨는 쪽을 연습할 수 없다는 비용을 감수했습니다. 부작용 하나: `players.length`가 항상
  1이 되면서 `WaitingBanner`가 오프라인 로비에 계속 떠 있는 회귀가 생겼습니다(예전엔
  봇 포함 3명이라 안 보였습니다) — 이번 문서화 커밋 바로 앞 커밋에서 온라인·오프라인
  구현이 각자 필요한 최소 인원(`minPlayers`: 온라인 `MIN_PLAYERS`=2, 오프라인 1)을
  `RoomInfo`로 직접 보고하게 고쳐 해결했습니다. `MIN_PLAYERS` 자체는 실서버 규칙이라
  바꾸지 않았습니다.

**계획이 지정한 검사 자체가 틀렸던 경우 둘.** 계획 문서는 증거가 아니라는 걸 보여주는
사례라 남겨둡니다 — 계획이 시킨 검사도 실제로 실패하는지 봐야 합니다.

1. `check:movement`에 추가하려던 합성 박스가 0.5 높이(발에서 0.5 위)였는데,
   `STEP_HEIGHT`가 0.45라 애초에 걸어 올라갈 수 없는 높이였습니다. `groundHeightAt`은
   그 박스를 무시하는 게 **맞는** 동작이라, 검사가 항상 실패하도록 짜여 있었습니다.
   0.4로 고쳐서 의도(계단으로 오를 수 있는 낮은 박스)를 살렸습니다(`ed48bc9`).
2. `check:cell`의 밀봉 검사가 벽 없이도 통과했습니다. `worldHalfSize`로 `CELL_HALF`
   (`CELL_INNER/2 + playerRadius`)를 넘겼는데, `moveXZ`가 중심 좌표를
   `worldHalfSize - radius`로 clamp하다 보니 딱 `CELL_INNER/2`에서 멈춰서 — 벽
   슬래브를 전부 지워도 이 clamp만으로 검사가 통과했습니다. `worldHalfSize`를 방보다
   훨씬 먼 값으로 바꿔 clamp를 검사에서 제거하고, 벽 자체가 막는지를 보게
   고쳤습니다(`cbbe035`). 같은 커밋에서 천장도 서 있는 높이가 아니라 점프 정점
   기준으로 다시 계산했습니다 — 이 엔진엔 천장 충돌이 없어서 점프한 머리를 막는 건
   순전히 방 높이뿐입니다.

### 남은 미결

기존 5개(6차 세션 목록 그대로, Verse8 배포 후 확인) + 이번 세션이 늘린 것:

- 밸런스 — 숨기 30초/술래 90초가 88×88에서 맞는지 (README 알려진 한계 11번)
- 성능 — 아레나 모델 621개, 인스턴싱 없음 (README 알려진 한계 12번)
- **`hiding → seeking` 순간이동은 아직 검증되지 않았습니다 — 서버 테스트도, 화면 확인도
  없습니다.** 같은 테스트 하네스가 리더보드·코인 적립에서 라운드를 결과 페이즈까지
  못 돌린 전례가 있어(같은 벽으로 추정) 자동 검증을 시도하지 않았고, 이번 세션은 브라우저
  패널로 직접 확인하는 단계까지 가지 못했습니다. `npm run check`(서버 테스트 22개 포함)와
  `npx vite build`만 돌렸습니다. **다음 세션이 할 일**: 로비에 계정 2개로 입장해 둘 다
  준비 완료 후 라운드를 시작하고, 숨기 페이즈가 끝나는 순간 술래가 셀에서 아레나 중앙
  `[0,0,0]`으로 옮겨지는지 화면으로 직접 지켜볼 것.

### 검증

```bash
npm run check      # 타입체크 + check:sync + check:bodies + check:shop + check:movement
                    # + check:hub + check:map + check:cell + check:audio + check:leaderboard
                    # + server:test(22개) — 전부 통과
npx tsc --noEmit    # 클린
npx vite build      # 성공
```

---

## 6차 세션 (2026-07-28) — 상점 상호작용 재설계 [완료]

5차 세션이 남긴 작업 지시서를 그대로 실행했습니다. 지시서의 기하 계산은 전부 그대로
들어맞았고, 계산해둔 값과 실제 코드 사이에 어긋난 곳은 없었습니다.

### 사용자 요청 (5차 세션에서 확정)

> "상점을 로비에서 3d구조물로 배치해서 아바타 앞에 서면 구매하는 방식하고싶은데"

모달 패널이 정작 미리보기인 마네킹을 가리던 문제. 상호작용은 **[E] 한 번**으로 확정
(`AskUserQuestion`으로 선택됨 — 아바타는 영구 해금이라 확인 단계 불필요).

### 한 일

**폐기:** `game/src/ui/Shop.tsx`, `ui.css`의 `.shop-*` 모달 블록, `atShop()`,
`SHOP.triggerRadius`, `onShopProximity`, `shopOpen`/`shopDismissed`/`nearShop` 상태.
상점은 더 이상 이동을 얼리지 않고 커서도 필요로 하지 않습니다.

**`game/src/hub/hubMap.ts`** — `STAND` 상수(`spacing 2.2` / `triggerRadius 1.0` / `stepZ 1.5`),
`Stand` 인터페이스, `BODIES`에서 파생한 `STANDS`, 최근접 우선 `standAt()`. 카운터 박스 2개를
백드롭 1개(폭은 `(BODIES.length - 1) * spacing + 1.6` = 8.2로 파생)로 교체하고, 맨 왼쪽 발판
트리거를 덮던 소품 `[-12, 6, ...]`을 삭제했습니다.

**`game/src/hub/Hub.tsx`** — 마네킹 4개(`classic` 포함)가 `STANDS`를 순회하고, 발판마다 바닥에
트리거 원을 그립니다. 현재 장착 중인 발판은 초록으로 강조.

**`game/src/hub/HubPlayer.tsx`** — `standRef`에 매 프레임 기록(`portalRef`와 같은 폴링 패턴).

**`game/src/ui/ShopPrompt.tsx` (신규)** — 비모달 하단 프롬프트, `pointer-events: none`,
`e.repeat` 가드가 있는 `KeyE` 리스너. 리스너는 `buy`/`equip`이 실제로 가능할 때만 등록됩니다.

**`game/src/ui/standAction.ts` (신규, 지시서에 없던 추가)** — `[E] 한 번이 무엇을 하는가`를
순수 함수로 분리했습니다: `loading`/`equipped`/`equip`/`buy`/`broke`. 모달이 사라지면서
"무엇이 가능한지"를 알려주던 비활성 버튼이 전부 없어졌기 때문에, 이 결정표가 곧 상호작용
전부입니다. `check:shop`이 9개 케이스로 검증합니다.

**`game/src/ui/useWallet.ts` (신규)** — 지갑 + busy + 자동 소멸 메시지를 한 훅으로. 4차 세션의
교훈 유지: **`buyAvatar`가 반환한 지갑을 그대로 쓰고 절대 재조회하지 않습니다.**

**`game/src/ui/HubHud.tsx`** — `standRef` 폴링을 기존 60ms `portalRef` 인터벌에 합치고, 상단 바에
코인 칩 상시 표시. **`game/src/App.tsx`** — 상점 상태 4개가 `useWallet` 하나로.

**`scripts/check-hub.ts`** — 상점 검사를 발판별로 재작성(설 수 있는 자리인지 / 스폰에서
도달 가능한지 / 트리거끼리 안 겹치는지 / 포털과 안 겹치는지). `STANDS`가 `BODIES`에서
파생되므로 **다섯 번째 체형을 추가하면 열이 넓어져 소품과 충돌할 수 있고, 이 검사가 그걸
잡습니다.** **`scripts/check-shop.ts`** — `standAction` 결정표 9개 검사 추가.

### 이어서 — 리더보드도 같은 방식으로 (사용자 요청)

> "리더보드도 아바타상점처럼 구조물처럼 표시해줘"

상점과 똑같은 처리를 했습니다. HUD 패널(`game/src/ui/Leaderboard.tsx`, `.leaderboard-*` CSS)을
폐기하고, 카펫 건너 상점과 대칭 위치(`x: 9.5, z: 4`)에 **기념비**를 세웠습니다.

- **`game/src/hub/leaderboardFace.ts` (신규)** — 판면을 그리는 순수 캔버스 코드. 컴포넌트에서
  분리한 이유는 검증입니다: 3D 씬은 애니메이션 프레임 안에서만 그려지는데 이 코드는
  프레임이 필요 없어서, **렌더러를 띄우지 않고 판면만 이미지로 뽑아 눈으로 확인**할 수
  있습니다. 실제로 그렇게 확인했습니다(아래 검증 참고).
- **`game/src/hub/LeaderboardBoard.tsx` (신규)** — 캔버스 텍스처를 판면 평면에 붙입니다.
  `NameTag`와 달리 빌보드가 아니고 depth test를 **합니다** — 물리적 물체의 표면이라
  가려지고 같이 돌아야 합니다. 조명 무관하게 대비를 유지하려고 `meshBasicMaterial`.
- **`game/src/ui/useLeaderboard.ts` (신규)** — 폴링을 컴포넌트 밖으로. 판면은 이제 월드
  지오메트리라 시선을 돌리면 언마운트되는 컴포넌트에 폴링을 두면 매번 재시작됩니다.
  `enabled`로 매치 중에는 안 돕니다.
- `hubMap.ts` — `LEADERBOARD` 상수 + 판/받침 박스. 소품 `[12, 6, ...]`도 삭제했습니다
  (판 앞을 가림). 5차 세션에 왼쪽 소품을 지웠으니 이걸로 다시 대칭이 됐습니다.
- `check-hub.ts` — 판 앞에 설 자리가 있는지 / 스폰에서 걸어갈 수 있는지 / 판이 상점 열에
  닿지 않는지. 상점 폭이 `BODIES`에서 파생되므로 마지막 검사가 의미가 있습니다.

### 검증 — 눈으로 확인 완료

`npm run check`(타입체크 + 8개 검사 스크립트 + 서버 테스트 22개) 전부 통과,
`npx vite build` 성공. `check:hub`는 발판 4개 각각에 대해 스폰에서 실제 물리로 걸어가
0.30~0.39u 안에 도달함을 확인합니다(트리거 반경 1.0).

**4세션 연속 막혀 있던 시각 확인이 이번에 뚫렸습니다.** 원인은 미리보기 도구의 버그가
아니라, **브라우저 패널이 화면에 표시되지 않으면 페이지가 백그라운드 탭이 되어
`document.hidden === true`, `requestAnimationFrame`이 초당 0회**라는 것이었습니다.
이 게임에서 움직이고 그려지는 것은 전부 `useFrame` 위에 있어서 아무것도 돌지 않고,
R3F는 아예 마운트조차 하지 않습니다(캔버스가 기본 300x150에 루트 0개). 패널을 띄우면
전부 정상입니다. 다음에 또 "미리보기가 안 된다"고 느끼면 버그를 찾지 말고 패널부터 켜세요.

에이전트가 화면에서 확인한 것: 마네킹 4개가 서로 다른 체형으로 보이고 라벨·발판 원이
맞으며 장착 중인 발판만 초록인 것, 씬 좌표가 계산값과 정확히 일치하는 것
(마네킹 태그 x = -12.8 / -10.6 / -8.4 / -6.2), 발판 프롬프트, `[E]` 구매(100 → 10 코인),
`[E]` 장착(`장착 중` + 초록 링 이동 + 몸 교체), 코인 부족 상태, 프롬프트가 항상 1개인 것.

**사용자가 직접 수동 테스트로 확인한 것:** 포인터 락(참가 직후 자동 획득 → Esc로 해제 →
클릭으로 재획득), 매치 왕복 후 페인트가 지워지는 것, 클래식 발판으로 기본 체형 복귀,
멈춤 없음, 상점 플로우 전체.

리더보드 판면은 `leaderboardFace.ts`를 페이지에서 직접 import해 PNG로 뽑아 확인했습니다 —
캔버스 2D는 애니메이션 프레임이 필요 없어서 렌더러 없이도 됩니다. 이 방법은 앞으로도
유효하니 판면 레이아웃을 바꿀 때 쓰세요.

**측정으로 잡은 실제 결함 하나:** 판이 처음엔 5.0x3.2였는데, 읽는 자리에서 화면상
115px밖에 안 나와 한글 글자가 6px였습니다. 판을 **서는 자리** 기준으로 키운 게 잘못이고,
실제로는 3인칭 카메라가 그보다 `CAMERA.playDistance`만큼 더 뒤에 있어 약 6.5u에서
읽힙니다. 줄 수를 10 → 5로 줄이고(줄 수가 곧 글자 크기입니다) 폰트 30 → 52, 판
5.6x4.4로. 재측정 11.1px. **판 크기를 다시 만질 일이 있으면 서는 자리가 아니라 카메라
거리로 계산하세요.**

### 이어서 — 실행해보고 나서야 나온 버그 4개

전부 화면에서 돌려봤기 때문에 나온 것들입니다. 코드 리뷰로는 안 나왔을 종류입니다.

**1. 참가 즉시 게임이 멈춤 (`18279d0`).** 오프라인 리허설 리그(`game/src/net/offline.ts`)는
원격 함수를 **매 렌더 새 화살표 함수**로 만듭니다 — 현재 state를 클로저에 담아야 해서
구조상 그렇습니다. 그걸 effect 의존성에 넣으면 fetch → `setState` → 리렌더 → 새 신원 →
effect 재실행 → 무한 루프. 값이 틀리는 게 아니라 **메인 스레드가 굶어서 화면이 멈춥니다.**
`useWallet`은 마운트부터, `useLeaderboard`는 `inHub`가 true 되는 순간부터 돌아서 정확히
참가 시점에 터졌습니다. 둘 다 원격 함수를 ref로 읽게 고쳤습니다.

> **이 함정은 앞으로도 유효합니다.** `game`에서 꺼낸 함수는 **절대 effect 의존성에 넣지
> 마세요.** 같은 이유로 페인트 flush 인터벌도 `[game]`에 걸려 있어서 매 렌더 140ms 타이머가
> 리셋되고 있었습니다(루프는 아니지만 타이머가 발화 못 함). 같이 고쳤습니다.

**2. 로비 복귀 시 페인트 잔류 (`18c5e97`).** 페인트 삭제가 숨기 페이즈 시작에만 걸려
있었고, 그 effect가 `if (inHub || phase !== "hiding") return`으로 **허브를 명시적으로
제외**합니다. 허브 진입 시 `clearAllSurfaces()` 추가.

**3. 리더보드 글씨가 안 읽힘 (`ab33091`).** 위 검증 절 참고.

**4. Esc 후 클릭으로 마우스를 다시 못 잡음 (`2ac2c30` → `b84bc66`).** 락을 캔버스 클릭
때만 시도해서 참가하면 락이 안 걸린 채 시작됐습니다. 마운트 직후에도 시도하게 바꿨는데
(참가 클릭 직후라 activation이 남아 있음), 그러자 진짜 버그가 드러났습니다 — **Chrome은
사용자가 Esc로 락을 빠져나온 직후 잠깐 재획득을 막는데, 그 일시적 거부를
`lockAvailable = false`로 영구 저장**하고 있었습니다. Esc 한 번에 마우스를 영영 다시 못
잡습니다. **거부는 절대 캐시하지 않도록** 고쳤고(`lockAvailable` 자체를 제거), 클릭할
때마다 다시 요청합니다.

같이: **커서는 락을 실제로 쥐고 있을 때만 숨깁니다.** 자유 시점에서는 커서가 캔버스를
벗어나면 시점 회전이 멈추는데(`e.target !== canvas`), 그 커서를 숨기면 플레이어는 자기
마우스룩을 무력화한 커서가 어디 있는지 알 수 없습니다.

### 남은 미결

4차 세션이 남긴 미결 3개 중 **1번(패널이 마네킹을 가림)과 3번(시각 확인)은 사라졌습니다.**
2번(실루엣 면적 미균일)은 설계 의도로 문서화되어 있습니다.

남은 것은 전부 **Verse8 배포 후에만 확인 가능한 것들**입니다 — 사용자가 배포 후 직접
확인하기로 했습니다:

1. **실제 네트워크 멀티플레이어.** 5세션 연속 미검증. 여기서 **가장 먼저 볼 것은 상대
   몸에 내 붓질이 보이는지**입니다 — 위 1번에서 페인트 flush 인터벌을 건드렸는데,
   오프라인엔 진짜 피어가 없어 전송 경로를 한 번도 못 탔습니다.
2. 라운드 종료 코인 적립 (테스트 하네스가 라운드를 끝까지 못 돌려 자동 검증 불가).
3. `wallets` / `leaderboard` 콜렉션의 영속성 (로컬 구현은 인메모리).
4. 리더보드는 Verse8 에디터에서 공식 문서 확인 후 진행 예정.
5. 포인터 락이 Verse8 임베드(iframe)에서 거부되는지.

---

## 4차 세션 (2026-07-27) — 아바타 상점 구현

프로젝트: Verse8/Agent8용 3D 멀티플레이어 숨바꼭질. 원작(Poki의 Hide and Paint)을 카피하지
않고 독자 구현. 로비(허브)+매치 두 씬, 자기 몸에 자유형 브러시로 페인팅, 서버 권한 페이즈 머신.

**이번 세션에 아바타 상점을 처음부터 끝까지 완성했습니다** — 지난 세션에서 브레인스토밍
중단 상태였던 기능입니다. 설계 문서, 계획, subagent-driven-development로 10개 태스크(문서
갱신 포함)를 순서대로 실행해 전부 커밋까지 마쳤습니다.

## 이번 세션에서 한 일 — 아바타 상점

**설계 문서**: [`docs/superpowers/specs/2026-07-27-avatar-shop-design.md`](docs/superpowers/specs/2026-07-27-avatar-shop-design.md)
**계획**: [`docs/superpowers/plans/2026-07-27-avatar-shop.md`](docs/superpowers/plans/2026-07-27-avatar-shop.md)
**커밋**: `2e4f85e`~`c6e6932`, 총 18개 (`git log --oneline 7bf8d33..HEAD`로 전체 목록 확인
가능). 뒤쪽 `ab0d8a0`~`c6e6932` 7개는 최종 리뷰가 잡아낸 문제들의 수정 웨이브입니다 —
그 중 `AVATAR_PRICES`가 평범한 객체 리터럴이라 `applyPurchase(w, "toString")`이 세 가지
거부 검사를 전부 통과하고 `coins: NaN`을 써버리던 실제 보안 버그가 있었습니다
(`hasOwnProperty` + `Number.isFinite` 가드로 수정).

지난 세션에서 결정하지 못했던 두 가지가 이번에 정해졌습니다:

- **아바타는 체형을 바꿉니다** (지난 세션에 기울어 있던 "텍스처 프리셋" 방향이 아니라
  체형 방향으로 결정됨). 대신 지난 세션이 우려했던 "체형이 다르면 위장 성능이 달라진다"는
  문제를 정면으로 풀었습니다 — 아래 참고.
- **화폐는 리더보드 점수와 분리된 별도의 "코인"** 입니다. 구매가 순위를 깎지 않습니다.

### 체형 4종, 전부 증명 가능하게 동일한 판정 크기

`game/src/game/bodies.ts`에 `classic`(기본, 무료) / `bean`(콩이, 40) / `stick`(막대, 60) /
`tank`(떡대, 90) 네 프로필. 총 키(`TOP_Y` 1.86), 발 높이(`FOOT_Y` 0.13), 최대 반폭
(`maxHalfWidth` ≤ `MOVE.playerRadius` 0.45)이 네 체형 전부 동일합니다.

- 키·발 높이는 프로필에 **저장하지 않고 파생(`derive()`)** 시켜서, 애초에 위반을 표현할
  방법이 없습니다.
- 폭과 어깨/엉덩이 피벗 위치는 저장값이라 위반이 가능하므로 `validateProfile()`이 검사하고,
  `scripts/check-bodies.ts`(`npm run check:bodies`)가 카탈로그 전체에 대해 돌립니다.
- `Humanoid.tsx`는 모듈 상수 대신 `BodyProfile`을 받아 렌더링하도록 바뀌었습니다
  (`26d25bc`).

숨바꼭질에서는 몸이 작을수록 은신에 유리하므로, 이건 우연한 일치가 아니라 **유료 아바타가
위장 성능에서 이득을 주지 않는다는 것을 코드로 증명**하려는 설계입니다.

### 코인 · 지갑 · 구매

- `COINS = { perRound: 5, survived: 5, perCatch: 2 }` (`server/src/rules.ts`). 라운드
  참여만 해도 5코인, 잡히지 않고 생존하면 +5, 술래는 잡은 명수당 +2.
- 순수 로직(`applyPurchase`/`applyEquip`/`coinsFor`/`parseOwned`/`serializeOwned`)은
  전부 `server/src/rules.ts`에 있고 `scripts/check-shop.ts`(`npm run check:shop`)가
  커버합니다.
- 서버: `wallets` 콜렉션(`WALLET_COLLECTION`), 원격 메서드 `getWallet`/`buyAvatar`/
  `equipAvatar`(`server/src/server.ts`). 구매·장착은 전부 `$lock("wallet:"+account)`로
  감싸 동시 요청 경쟁을 막습니다.
- 코인 적립은 `endRound`에 들어가 있고, 리더보드 쓰기와 **같은 계약**을 따릅니다 — 페이즈를
  먼저 `results`로 전환한 뒤 지갑 쓰기를 시도하고, 실패해도 라운드 진행을 막지 않습니다
  (커밋 `88d005c`의 그 교훈을 그대로 재사용).
- 클라: `WalletView`/`BuyFailure` 타입(`game/src/net/types.ts`), 오프라인 리허설 모드는 코인
  100개로 시작하는 인메모리 지갑을 흉내냅니다.

### 허브 상점 키오스크 + 구매 패널

- `game/src/hub/hubMap.ts`의 `SHOP`(좌표 `x:-9.5, z:4`, `triggerRadius: 3.0`)과 `atShop()`.
  스폰 지점과 포털 사이, 카펫 옆에 배치해서 일부러 찾아가지 않아도 지나가게 됩니다.
- 유료 세 체형(`bean`/`stick`/`tank`)이 실제 크기 마네킹으로 턴테이블 위에 서 있습니다
  (`0afb866`).
- 반경 3.0 안으로 걸어 들어가면 `HubPlayer`가 `onShopProximity`로 알리고 `App.tsx`가
  `Shop.tsx` 패널을 열면서 허브 플레이어를 얼립니다(`frozen`). 패널은 닫기 버튼 또는
  Escape로 닫히고(`dismissed`), 그러면 다시 움직일 수 있습니다.
- `Shop.tsx`: 구매 실패 사유는 `REFUSAL` 맵(`unknown`/`owned`/`broke`)으로 인라인
  메시지, 네트워크 예외는 조용히 재시도 유도 메시지, 미확인 폴백 없이 항상 4종 카드를
  보여줍니다.

## 아직 안 된 것 / 검증 못 한 것 — 정직하게

이번 세션에서 **브라우저 미리보기 패널이 이 환경에서 작동하지 않았습니다** (뷰포트가
0x0으로 잡히고 스크린샷이 타임아웃). 그래서:

1. **상점 마네킹 3개가 눈으로 봤을 때 실제로 서로 다른 체형으로 보이는지, 패널
   레이아웃이 의도대로 나오는지, 구매→장착 전체 플로우가 화면에서 실제로 동작하는지는
   이번 세션에서 한 번도 시각적으로 확인되지 못했습니다.** 대신 기하학적 산술(높이·폭·
   피벗 계산)과 상태 머신 트레이스(구매→장착→재구매 불가 등)로 대체 검증했고, 둘 다
   독립적으로 재검증했습니다. 하지만 이건 실제로 화면을 본 것과 같지 않습니다. **다음
   세션에서 브라우저 패널이 정상 작동하면 가장 먼저 이 세 가지를 실제로 눈으로 확인하세요.**
2. **라운드 종료 시 코인 적립은 자동 검증되지 않습니다.** `@agent8/gameserver-node`
   테스트 하네스가 라운드를 결과 페이즈까지 진행시키지 못하는 것과 같은 벽(리더보드도
   똑같은 사정)입니다. `coinsFor`의 산술은 `check:shop`이 커버하지만, `endRound`가 그
   값을 실제로 지갑에 반영하는 배선은 코드 리뷰로만 확인했습니다. 같은 이유로 **구매
   자체도 살아있는 서버 테스트에서는 재현 불가능**합니다 — 테스트가 스스로에게 코인을
   지급할 원격 메서드가 없기 때문인데(만들면 누구나 자기 잔액을 조작할 수 있게 되므로
   의도적으로 안 만들었습니다), 그래서 구매 성공 경로도 순수 로직 검증과 코드 리뷰로만
   확인된 상태입니다.
3. **`wallets` 콜렉션의 실제 배포 환경 영속성 미확인** — 리더보드 콜렉션과 정확히 같은
   사정입니다(로컬 구현은 인메모리, 서버 재시작 시 소실). 배포 후 확인 필요.
4. **실제 네트워크 멀티플레이어(두 진짜 클라이언트 간 동기화)는 이번 세션도 검증
   못했습니다 — 4세션 연속입니다.** `.env` 파일 자체가 존재하지 않아 `VITE_AGENT8_VERSE`가
   비어 있고, 앱은 항상 오프라인 리허설 모드로 뜹니다(`game/src/net/useGame.ts:20`). 실서버를
   붙여서 두 브라우저 탭(또는 두 기기)으로 접속해보는 것이 여전히 남아 있습니다.

README의 "3D 로비 허브 미구현" 항목은 이번 세션에서 바로잡았습니다 — 허브는 오래전부터
동작하고 있었고, 그 문구 자체가 사실과 달랐습니다. README 알려진 한계 섹션에 아바타 상점
관련 3개 항목(코인 적립 미검증, 지갑 영속성 미확인, 체형 판정 크기 불변식)도 추가했습니다.

## 다음 세션에 할 만한 일

우선순위 순:

1. **Verse8 배포 후 확인** — 위 "남은 미결" 5개. 특히 멀티플레이어에서 상대 몸에 붓질이
   보이는지가 최우선입니다.
2. **테스트는 사용자가 직접 합니다** (6차 세션에서 그렇게 정했습니다). 에이전트는 화면
   확인이 필요하면 브라우저 패널을 띄워달라고 요청하고, 무엇을 어떤 순서로 눌러야 하는지
   기대 결과와 함께 목록으로 주세요.
3. Y축 이동 서버 검증 — `updateTransform`이 XZ만 clamp하고 Y는 무검증이라 수직
   순간이동/부양이 통과합니다. 중력 낙하를 오탐하지 않는 별도 판정이 필요합니다. 순수
   로직이라 `check:*` 스크립트로 완전 자동 검증이 가능한 항목입니다.
4. README 다음 단계 항목들(허브 접속자 표시, 포털 자동매칭, 페인트 중간 참여,
   `$roomTick` 실측, 모바일)은 전부 그대로 남아 있습니다.
5. 새 기능을 또 시작한다면 지난 세션들과 같은 순서로: `superpowers:brainstorming` →
   설계 문서 승인 → `superpowers:writing-plans` → `superpowers:subagent-driven-development`.

**6차 세션의 교훈 하나:** 이 프로젝트는 순수 로직 검증(`check:*` 8개)이 두터워서 코드
리뷰와 스크립트만으로도 꽤 멀리 갑니다. 그런데 이번에 나온 버그 4개는 **전부 실제로
실행해봐야만 나오는 종류**였습니다(무한 렌더 루프, 페이즈 전환 시 상태 잔류, 화면상 글자
크기, 브라우저의 포인터 락 쿨다운). 검사 스크립트가 통과한다고 동작한다는 뜻이 아닙니다.

## 검증 명령

```bash
npm run check          # 타입체크 + 맵/속도 동기화 + 체형 불변식 + 상점 순수 로직
                        # + 이동/카메라/점프/코너탈출 + 허브 도달성
                        # + 사운드/리더보드 순수 로직 + 서버 테스트 — 전부 통과 상태
npx tsc --noEmit        # 클린
npx vite build          # 성공
npm run check:bodies    # 체형 4종 불변식(키/발높이/반폭/어깨·엉덩이 피벗) 개별 확인
npm run check:shop      # 코인 계산·구매·장착 순수 로직 개별 확인
```

## 알려진 한계 (README.md에 최신 상태로 정리돼 있음, 요약)

1. ~~3D 로비 허브 미구현~~ — 이번 세션에 바로잡음: 허브는 이미 구현되어 있습니다. 남은
   건 접속자 표시·포털 자동매칭.
2. 수평(XZ) 이동만 서버 검증됨, Y축은 아직.
3. 페인트 중간 참여 미지원.
4. `$roomTick` 실제 주기 미측정 — 배포 후 확인 필요.
5. 모바일 미지원 / VXShop·Ads 미연동.
6. 리더보드 콜렉션의 실제 배포 환경 영속성 미확인.
7. 아바타 상점: 코인 적립 자동 검증 불가(테스트 하네스 한계), 구매도 마찬가지.
8. `wallets` 콜렉션의 실제 배포 환경 영속성 미확인.
9. 아바타 체형 판정 크기 불변식 — 설계 의도이며 `check:bodies`로 코드 차원에서 강제됨
   (한계라기보다 이번 세션이 남긴 보증에 가깝지만, 다음 체형을 추가할 때 반드시 지켜야
   할 제약이라 여기 같이 적어둡니다).
10. 실제 네트워크 멀티플레이어 미검증 — 5세션 연속. 배포 후 확인 예정.
11. ~~상점 UI/마네킹의 시각적 동작 미확인~~ — 6차 세션에 해소. 에이전트가 화면에서
    확인했고 사용자가 수동 테스트로 재확인했습니다. 막고 있던 것은 도구 버그가 아니라
    **브라우저 패널이 표시되지 않으면 페이지가 백그라운드 탭이 되어 `requestAnimationFrame`이
    아예 돌지 않는다**는 것이었습니다(R3F는 마운트조차 안 함). 패널을 띄우면 됩니다.

---

## 구조 변경 — `game/`이 Vite root입니다 (2026-07-30)

`index.html`과 `src/`가 `game/` 안으로 들어갔습니다. Verse8이 그 구조라는 프로젝트 소유자의
지시입니다.

```
game/index.html      Vite root의 진입점
game/src/            클라이언트 전체
public/              정적 에셋 (game/ 밖, vite.config의 publicDir가 가리킴)
server/              서버 (번들 대상 아님)
scripts/             검사 스크립트 (번들 대상 아님)
dist/                빌드 산출물
```

`src/`가 같이 움직여야 했던 이유는 Vite가 root 밖의 모듈을 서빙하지 않고, `index.html`의
스크립트 태그가 root 기준 `/src/main.tsx`이기 때문입니다. `publicDir`와 `outDir`는 `game/`
밖을 가리켜 `public/`·`dist/`는 제자리에 있습니다 — 에셋 URL과 배포 단계가 그대로 동작합니다.

검사 스크립트 31곳의 import가 `../game/src/`로 바뀌었고, `tsconfig.json`의 `include`도
`game/src`가 됐습니다.

**정직하게 남기는 두 가지.** Vite 공식 문서는 `index.html`을 프로젝트 루트에 두라고 하고,
Verse8 문서에서는 `game/` 요구사항을 찾지 못했습니다 — 시작하기 문서는 `src/`와 `public/`을
루트에 둔 구조를 보여주고, 클라이언트 빌드·퍼블리싱 페이지 자체가 없습니다. 배포가 이 구조를
거부하면 되돌리는 건 커밋 하나입니다.

**배포는 여전히 미검증입니다.** `.env`가 없어 `VITE_AGENT8_VERSE`가 비어 있고 앱은 항상
오프라인으로 뜹니다. 이 구조가 Verse8에서 실제로 도는 것은 아무도 확인하지 않았습니다.
