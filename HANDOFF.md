# 인수인계 (2026-07-27, 3차 세션)

새 세션에서 이 파일을 먼저 읽고 시작하세요. [README.md](README.md)는 아키텍처 배경(왜 이렇게
만들었는지)을 설명하고, 이 파일은 **지금 뭘 하고 있었는지**를 설명합니다.

프로젝트: Verse8/Agent8용 3D 멀티플레이어 숨바꼭질. 원작(Poki의 Hide and Paint)을 카피하지
않고 독자 구현. 로비(허브)+매치 두 씬, 자기 몸에 자유형 브러시로 페인팅, 서버 권한 페이즈 머신.

**이번 세션에 브라우저 패널이 정상 작동했습니다** — 지난 두 세션의 "시각 확인 필요" 항목
3개(눕기 자세, 코너 탈출, 점프+자세메뉴 공중정지)를 실제로 눈으로 확인 완료. 다만 오프라인
모드(봇 2명, `미나`/`준`)로만 확인했고 — **실제 네트워크 멀티플레이어(두 진짜 클라이언트
간 동기화)는 이번 세션도 검증 못했습니다**. `.env`에 `VITE_AGENT8_VERSE`가 없으면 앱이
자동으로 오프라인 리허설 모드로 뜹니다(`src/net/useGame.ts:20`).

**이번 세션에 git 저장소를 처음 초기화했습니다** (`git init`, 지금까지는 저장소가 아니었음).
서브에이전트 기반 개발(subagent-driven-development)로 3개 기능을 완주했고, 커밋 21개가
`master`에 그대로 남아있습니다. 원격 저장소는 없습니다.

## 이번 세션에서 한 일

### 1. 지난 세션 버그 3건 — 브라우저로 최종 확인 완료

눕기 자세, 코너 탈출, 점프+자세메뉴 공중정지 전부 오프라인 모드에서 직접 재현해 정상
동작 확인. 코드 변경 없음(이미 지난 세션에 수정 완료된 상태였음).

### 2. 이동 서버 검증 (커밋 `0eefaed`~`cfc9893`)

`README.md`가 명시한 "이동이 서버 검증 안 됨(스피드핵/텔레포트 가능)" 항목을 해결.
`server/src/server.ts`의 `updateTransform`이 이제 직전 위치·경과시간 기준 XZ 이동거리를
clamp합니다. 서버 상수는 `server/src/rules.ts`(`MOVE_SPEED_CAP`, `SPEED_GRACE`, `MIN_DT_MS`,
`MAX_DT_MS`), `scripts/check-sync.ts`가 클라 속도 상수와 동기화 검사.

- 설계 문서: [`docs/superpowers/specs/2026-07-24-movement-validation-design.md`](docs/superpowers/specs/2026-07-24-movement-validation-design.md)
- 최종 리뷰에서 잡은 진짜 버그: `elapsed`에 상한이 없어서 오래 가만히 있다가 움직이면
  사실상 무제한 순간이동이 허용되던 문제(30초 대기 후 306유닛) — `MAX_DT_MS=1000` 상한으로
  해결(`426bbbc`).
- Y축(수직) 이동은 의도적으로 범위 밖(점프/낙하 순간속도가 커서 정상 낙하 오탐 위험,
  README에 남겨둠).

### 3. 사운드 이펙트 (커밋 `a57bbe4`~`d711827`)

캐치·페인트 브러시·라운드 전환(시작/결과) + 음소거 토글. 전부 `src/audio/sound.ts`에서
Web Audio API로 코드 합성 — 외부 음향 파일 없음(3D 에셋과 같은 원칙, README 저작권 안내
참고).

- 설계 문서: [`docs/superpowers/specs/2026-07-24-sound-effects-design.md`](docs/superpowers/specs/2026-07-24-sound-effects-design.md)
- 태스크 중간에 뮤트 버튼이 기존 힌트 패널과 겹치는 CSS 버그 발견·수정.
- 최종 리뷰에서 잡은 버그: `unlockAudio()`가 AudioContext를 생성만 하고 resume을 안 해서
  iOS/Safari에서 무음 위험 — 수정(`d711827`).
- 점프/착지 사운드는 사용자가 명시적으로 범위에서 제외.

### 4. 로비 리더보드 (커밋 `6d8d3fe`~`88d005c`)

허브 화면 우측 상단에 누적 총점수 기준 상위 10명 + 내 순위(10위 밖일 때). 서버가
`$global` 콜렉션(`"leaderboard"`, 방과 무관한 영구 저장소, SDK 기본 제공)에 라운드
종료마다 계정별 점수를 누적.

- 설계 문서: [`docs/superpowers/specs/2026-07-24-leaderboard-design.md`](docs/superpowers/specs/2026-07-24-leaderboard-design.md)
- 의도적으로 하지 않은 것: 리더보드에 점수를 임의로 주입하는 진단용(diagnostic) write
  메서드를 테스트 편의로 추가하지 않음 — 그건 누구나 자기 점수를 조작할 수 있는 진짜
  보안 구멍이 됨. 그래서 자동 테스트는 순수 랭킹 로직(`attachRanks`)과 빈 상태만 커버.
- 최종 리뷰에서 잡은 중요한 버그: `endRound`가 리더보드 콜렉션에 쓰기를 먼저 하고
  라운드를 `results` 페이즈로 전환하는 게 나중이었음 — 콜렉션 쓰기가 실패하면 라운드가
  `seeking`에서 영원히 못 빠져나오고(`$roomTick`이 같은 조건으로 계속 재시도), 부분
  성공한 쓰기는 재시도 때 중복 집계됨. **페이즈 전환을 먼저** 하고 리더보드 쓰기는
  `try/catch`로 감싸서 실패해도 라운드 진행을 막지 않도록 수정(`88d005c`).
- **미확인**: 실제 배포 환경에서 이 콜렉션이 서버 재시작을 넘어 영속되는지(로컬 구현은
  인메모리) — README에 남겨둠.

### 5. 아바타 상점 — 브레인스토밍 중단, 결정 필요

사용자가 "리더보드 + 새 아바타 구매"를 요청. 리더보드는 완료, **아바타 상점은 설계
초반에 세션이 끝났습니다.**

**조사한 것**: `src/game/Humanoid.tsx`를 읽어보니, 캐릭터 지오메트리(팔다리 길이/반지름,
`SHOULDER_Y`/`HIP_Y` 등)가 걷기·점프 애니메이션과 촘촘하게 얽혀 있고, `MOVE.playerRadius`
(충돌 판정)·`CAMERA.eyeHeight`/`shoulderHeight`(카메라)도 이 체형을 전제로 튜닝돼 있습니다.
**"체형이 다른 아바타"는 이 파일들을 전부 건드려야 하는 훨씬 큰 작업**이라는 걸 확인했습니다.

**대안으로 기울고 있던 방향**(사용자 승인 전 중단): 체형 대신 `src/game/paint.ts`의
`PaintSurface.clear()`(현재 항상 `BASE_COLOR` 단색으로 초기화)가 적용하는 **시작 텍스처
패턴/색상 프리셋**을 아바타로 파는 방향. 지오메트리·충돌·카메라를 전혀 안 건드리고,
페인팅 시스템과도 자연스럽게 어울림(구매한 패턴 위에 계속 자유형으로 덧칠 가능).

**아직 결정 안 된 것**:
1. 아바타가 정확히 뭘 바꾸는지(위 텍스처 패턴 프리셋 방향에 사용자 동의 필요).
2. 화폐를 리더보드의 누적 `total`을 그대로 쓸지(단점: 구매하면 순위가 깎임 — 경쟁 지표와
   충돌), 아니면 별도의 "코인" 화폐를 라운드마다 점수와 별개로 적립할지. 별도 화폐 쪽으로
   기울어 있었으나 미확정.
3. 상점 UI 위치/디자인.

**다음 세션 시작 시**: 브레인스토밍을 여기서부터 재개하세요. 사용자에게 "텍스처 패턴
프리셋 방향이 맞는지"와 "화폐를 별도로 만들지"부터 확인하고, `superpowers:brainstorming`
스킬로 설계 문서(`docs/superpowers/specs/`) 작성 → 계획(`docs/superpowers/plans/`) →
서브에이전트 실행 순서로 진행하면 됩니다(이번 세션의 다른 3개 기능이 정확히 이 순서로
진행됐으니 그대로 따라 하면 됩니다).

## 이번 세션에서 확립된 작업 방식 (다음 세션도 유지)

- 새 기능은 전부 `superpowers:brainstorming` → 설계 문서 승인 → `superpowers:writing-plans`
  → `superpowers:subagent-driven-development`(사용자가 매번 이 방식 선택)로 진행했습니다.
- 서브에이전트 리뷰가 실제로 여러 번 진짜 버그를 잡았습니다(위 4개 기능 각각 최소 1개씩).
  가벼워 보여도 최종 전체 브랜치 리뷰까지 꼭 거치세요.
- 진행 기록은 `.superpowers/sdd/progress.md`에 있습니다(git-ignored, 세션 로컬). git
  저장소가 이제 있으니 `git log`가 더 신뢰할 수 있는 기록입니다.
- git 저장소가 없던 프로젝트라 이번 세션에 `git init`했습니다 — 다음 세션은 이미 저장소인
  상태로 시작합니다.

## 검증 명령

```bash
npm run check          # 타입체크 + 맵/속도 동기화 + 이동/카메라/점프/코너탈출 + 허브 도달성
                        # + 사운드/리더보드 순수 로직 + 서버 테스트(16개) — 전부 통과 상태
npx tsc --noEmit        # 클린
npx vite build          # 성공
```

## 알려진 한계 (README.md에 최신 상태로 정리돼 있음, 요약)

1. 3D 로비 허브 미구현 — **이 항목 자체가 오래된 문구입니다, 실제로는 이미 구현되어 있음**
   (README를 고쳐야 할지는 아직 사용자와 상의 안 함).
2. 수평(XZ) 이동만 서버 검증됨, Y축은 아직.
3. 페인트 중간 참여 미지원.
4. `$roomTick` 실제 주기 미측정 — 배포 후 확인 필요.
5. 모바일 미지원 / VXShop·Ads 미연동.
6. 리더보드 콜렉션의 실제 배포 환경 영속성 미확인.
7. 포인터 락이 이 사용자 환경에서 거부됨 — 원인 미확인.
